// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: market/v1/book.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "market.v1";

export interface Level {
  price: string;
  quantity: string;
}

export interface Book {
  bids: Level[];
  asks: Level[];
  firstUpdateId: number;
  lastUpdateId: number;
  isSnapshot: boolean;
}

function createBaseLevel(): Level {
  return { price: "", quantity: "" };
}

export const Level: MessageFns<Level> = {
  encode(message: Level, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.price !== "") {
      writer.uint32(10).string(message.price);
    }
    if (message.quantity !== "") {
      writer.uint32(18).string(message.quantity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Level {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLevel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Level {
    return {
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
    };
  },

  toJSON(message: Level): unknown {
    const obj: any = {};
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Level>, I>>(base?: I): Level {
    return Level.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Level>, I>>(object: I): Level {
    const message = createBaseLevel();
    message.price = object.price ?? "";
    message.quantity = object.quantity ?? "";
    return message;
  },
};

function createBaseBook(): Book {
  return { bids: [], asks: [], firstUpdateId: 0, lastUpdateId: 0, isSnapshot: false };
}

export const Book: MessageFns<Book> = {
  encode(message: Book, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.bids) {
      Level.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.asks) {
      Level.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.firstUpdateId !== 0) {
      writer.uint32(24).int64(message.firstUpdateId);
    }
    if (message.lastUpdateId !== 0) {
      writer.uint32(32).int64(message.lastUpdateId);
    }
    if (message.isSnapshot !== false) {
      writer.uint32(40).bool(message.isSnapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Book {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBook();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bids.push(Level.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.asks.push(Level.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.firstUpdateId = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastUpdateId = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isSnapshot = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Book {
    return {
      bids: globalThis.Array.isArray(object?.bids) ? object.bids.map((e: any) => Level.fromJSON(e)) : [],
      asks: globalThis.Array.isArray(object?.asks) ? object.asks.map((e: any) => Level.fromJSON(e)) : [],
      firstUpdateId: isSet(object.firstUpdateId) ? globalThis.Number(object.firstUpdateId) : 0,
      lastUpdateId: isSet(object.lastUpdateId) ? globalThis.Number(object.lastUpdateId) : 0,
      isSnapshot: isSet(object.isSnapshot) ? globalThis.Boolean(object.isSnapshot) : false,
    };
  },

  toJSON(message: Book): unknown {
    const obj: any = {};
    if (message.bids?.length) {
      obj.bids = message.bids.map((e) => Level.toJSON(e));
    }
    if (message.asks?.length) {
      obj.asks = message.asks.map((e) => Level.toJSON(e));
    }
    if (message.firstUpdateId !== 0) {
      obj.firstUpdateId = Math.round(message.firstUpdateId);
    }
    if (message.lastUpdateId !== 0) {
      obj.lastUpdateId = Math.round(message.lastUpdateId);
    }
    if (message.isSnapshot !== false) {
      obj.isSnapshot = message.isSnapshot;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Book>, I>>(base?: I): Book {
    return Book.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Book>, I>>(object: I): Book {
    const message = createBaseBook();
    message.bids = object.bids?.map((e) => Level.fromPartial(e)) || [];
    message.asks = object.asks?.map((e) => Level.fromPartial(e)) || [];
    message.firstUpdateId = object.firstUpdateId ?? 0;
    message.lastUpdateId = object.lastUpdateId ?? 0;
    message.isSnapshot = object.isSnapshot ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
