// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: market/v1/bar.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Interval, intervalFromJSON, intervalToJSON } from "../../common/v1/enums";

export const protobufPackage = "market.v1";

export interface Bar {
  /** High-Frequency Data (Tags 1-8: Single Byte) */
  openPrice: string;
  highPrice: string;
  lowPrice: string;
  closePrice: string;
  baseVolume: string;
  quoteVolume: string;
  trades: number;
  isClosed: boolean;
  /** Timing & Identification (Tags 9-13) */
  startTimeNs: number;
  endTimeNs: number;
  interval: Interval;
  /** Taker-Specific Data (Tags 12-15) */
  takerBuyBaseVolume: string;
  takerBuyQuoteVolume: string;
  firstTradeId: number;
  lastTradeId: number;
  /** Extended Analytics (Tags 16+: Two Bytes) */
  vwap: string;
}

function createBaseBar(): Bar {
  return {
    openPrice: "",
    highPrice: "",
    lowPrice: "",
    closePrice: "",
    baseVolume: "",
    quoteVolume: "",
    trades: 0,
    isClosed: false,
    startTimeNs: 0,
    endTimeNs: 0,
    interval: 0,
    takerBuyBaseVolume: "",
    takerBuyQuoteVolume: "",
    firstTradeId: 0,
    lastTradeId: 0,
    vwap: "",
  };
}

export const Bar: MessageFns<Bar> = {
  encode(message: Bar, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.openPrice !== "") {
      writer.uint32(10).string(message.openPrice);
    }
    if (message.highPrice !== "") {
      writer.uint32(18).string(message.highPrice);
    }
    if (message.lowPrice !== "") {
      writer.uint32(26).string(message.lowPrice);
    }
    if (message.closePrice !== "") {
      writer.uint32(34).string(message.closePrice);
    }
    if (message.baseVolume !== "") {
      writer.uint32(42).string(message.baseVolume);
    }
    if (message.quoteVolume !== "") {
      writer.uint32(50).string(message.quoteVolume);
    }
    if (message.trades !== 0) {
      writer.uint32(56).int64(message.trades);
    }
    if (message.isClosed !== false) {
      writer.uint32(64).bool(message.isClosed);
    }
    if (message.startTimeNs !== 0) {
      writer.uint32(72).int64(message.startTimeNs);
    }
    if (message.endTimeNs !== 0) {
      writer.uint32(80).int64(message.endTimeNs);
    }
    if (message.interval !== 0) {
      writer.uint32(88).int32(message.interval);
    }
    if (message.takerBuyBaseVolume !== "") {
      writer.uint32(98).string(message.takerBuyBaseVolume);
    }
    if (message.takerBuyQuoteVolume !== "") {
      writer.uint32(106).string(message.takerBuyQuoteVolume);
    }
    if (message.firstTradeId !== 0) {
      writer.uint32(112).int64(message.firstTradeId);
    }
    if (message.lastTradeId !== 0) {
      writer.uint32(120).int64(message.lastTradeId);
    }
    if (message.vwap !== "") {
      writer.uint32(130).string(message.vwap);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Bar {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBar();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.openPrice = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.highPrice = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lowPrice = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.closePrice = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.baseVolume = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.quoteVolume = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.trades = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.isClosed = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.startTimeNs = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.endTimeNs = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.takerBuyBaseVolume = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.takerBuyQuoteVolume = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.firstTradeId = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.lastTradeId = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.vwap = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Bar {
    return {
      openPrice: isSet(object.openPrice) ? globalThis.String(object.openPrice) : "",
      highPrice: isSet(object.highPrice) ? globalThis.String(object.highPrice) : "",
      lowPrice: isSet(object.lowPrice) ? globalThis.String(object.lowPrice) : "",
      closePrice: isSet(object.closePrice) ? globalThis.String(object.closePrice) : "",
      baseVolume: isSet(object.baseVolume) ? globalThis.String(object.baseVolume) : "",
      quoteVolume: isSet(object.quoteVolume) ? globalThis.String(object.quoteVolume) : "",
      trades: isSet(object.trades) ? globalThis.Number(object.trades) : 0,
      isClosed: isSet(object.isClosed) ? globalThis.Boolean(object.isClosed) : false,
      startTimeNs: isSet(object.startTimeNs) ? globalThis.Number(object.startTimeNs) : 0,
      endTimeNs: isSet(object.endTimeNs) ? globalThis.Number(object.endTimeNs) : 0,
      interval: isSet(object.interval) ? intervalFromJSON(object.interval) : 0,
      takerBuyBaseVolume: isSet(object.takerBuyBaseVolume) ? globalThis.String(object.takerBuyBaseVolume) : "",
      takerBuyQuoteVolume: isSet(object.takerBuyQuoteVolume) ? globalThis.String(object.takerBuyQuoteVolume) : "",
      firstTradeId: isSet(object.firstTradeId) ? globalThis.Number(object.firstTradeId) : 0,
      lastTradeId: isSet(object.lastTradeId) ? globalThis.Number(object.lastTradeId) : 0,
      vwap: isSet(object.vwap) ? globalThis.String(object.vwap) : "",
    };
  },

  toJSON(message: Bar): unknown {
    const obj: any = {};
    if (message.openPrice !== "") {
      obj.openPrice = message.openPrice;
    }
    if (message.highPrice !== "") {
      obj.highPrice = message.highPrice;
    }
    if (message.lowPrice !== "") {
      obj.lowPrice = message.lowPrice;
    }
    if (message.closePrice !== "") {
      obj.closePrice = message.closePrice;
    }
    if (message.baseVolume !== "") {
      obj.baseVolume = message.baseVolume;
    }
    if (message.quoteVolume !== "") {
      obj.quoteVolume = message.quoteVolume;
    }
    if (message.trades !== 0) {
      obj.trades = Math.round(message.trades);
    }
    if (message.isClosed !== false) {
      obj.isClosed = message.isClosed;
    }
    if (message.startTimeNs !== 0) {
      obj.startTimeNs = Math.round(message.startTimeNs);
    }
    if (message.endTimeNs !== 0) {
      obj.endTimeNs = Math.round(message.endTimeNs);
    }
    if (message.interval !== 0) {
      obj.interval = intervalToJSON(message.interval);
    }
    if (message.takerBuyBaseVolume !== "") {
      obj.takerBuyBaseVolume = message.takerBuyBaseVolume;
    }
    if (message.takerBuyQuoteVolume !== "") {
      obj.takerBuyQuoteVolume = message.takerBuyQuoteVolume;
    }
    if (message.firstTradeId !== 0) {
      obj.firstTradeId = Math.round(message.firstTradeId);
    }
    if (message.lastTradeId !== 0) {
      obj.lastTradeId = Math.round(message.lastTradeId);
    }
    if (message.vwap !== "") {
      obj.vwap = message.vwap;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Bar>, I>>(base?: I): Bar {
    return Bar.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Bar>, I>>(object: I): Bar {
    const message = createBaseBar();
    message.openPrice = object.openPrice ?? "";
    message.highPrice = object.highPrice ?? "";
    message.lowPrice = object.lowPrice ?? "";
    message.closePrice = object.closePrice ?? "";
    message.baseVolume = object.baseVolume ?? "";
    message.quoteVolume = object.quoteVolume ?? "";
    message.trades = object.trades ?? 0;
    message.isClosed = object.isClosed ?? false;
    message.startTimeNs = object.startTimeNs ?? 0;
    message.endTimeNs = object.endTimeNs ?? 0;
    message.interval = object.interval ?? 0;
    message.takerBuyBaseVolume = object.takerBuyBaseVolume ?? "";
    message.takerBuyQuoteVolume = object.takerBuyQuoteVolume ?? "";
    message.firstTradeId = object.firstTradeId ?? 0;
    message.lastTradeId = object.lastTradeId ?? 0;
    message.vwap = object.vwap ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
