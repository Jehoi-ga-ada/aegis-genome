// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: market/v1/tick.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "market.v1";

export interface Tick {
  price: string;
  quantity: string;
  side: Tick_Side;
}

export enum Tick_Side {
  SIDE_UNSPECIFIED = 0,
  SIDE_BUY = 1,
  SIDE_SELL = 2,
  UNRECOGNIZED = -1,
}

export function tick_SideFromJSON(object: any): Tick_Side {
  switch (object) {
    case 0:
    case "SIDE_UNSPECIFIED":
      return Tick_Side.SIDE_UNSPECIFIED;
    case 1:
    case "SIDE_BUY":
      return Tick_Side.SIDE_BUY;
    case 2:
    case "SIDE_SELL":
      return Tick_Side.SIDE_SELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Tick_Side.UNRECOGNIZED;
  }
}

export function tick_SideToJSON(object: Tick_Side): string {
  switch (object) {
    case Tick_Side.SIDE_UNSPECIFIED:
      return "SIDE_UNSPECIFIED";
    case Tick_Side.SIDE_BUY:
      return "SIDE_BUY";
    case Tick_Side.SIDE_SELL:
      return "SIDE_SELL";
    case Tick_Side.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseTick(): Tick {
  return { price: "", quantity: "", side: 0 };
}

export const Tick: MessageFns<Tick> = {
  encode(message: Tick, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.price !== "") {
      writer.uint32(10).string(message.price);
    }
    if (message.quantity !== "") {
      writer.uint32(18).string(message.quantity);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tick {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTick();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.quantity = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tick {
    return {
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "",
      side: isSet(object.side) ? tick_SideFromJSON(object.side) : 0,
    };
  },

  toJSON(message: Tick): unknown {
    const obj: any = {};
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.quantity !== "") {
      obj.quantity = message.quantity;
    }
    if (message.side !== 0) {
      obj.side = tick_SideToJSON(message.side);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tick>, I>>(base?: I): Tick {
    return Tick.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tick>, I>>(object: I): Tick {
    const message = createBaseTick();
    message.price = object.price ?? "";
    message.quantity = object.quantity ?? "";
    message.side = object.side ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
