// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: analytic/v1/analytic_event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { RsiValue } from "./rsi";
import { StochasticValue } from "./stochastic";
import { StochasticRsiValue } from "./stochastic_rsi";

export const protobufPackage = "analytic.v1";

export interface AnalyticEvent {
  symbol: string;
  eventTimeNs: number;
  receivedTimeNs: number;
  rsiValue?: RsiValue | undefined;
  stochasticRsiValue?: StochasticRsiValue | undefined;
  stochasticValue?: StochasticValue | undefined;
}

function createBaseAnalyticEvent(): AnalyticEvent {
  return {
    symbol: "",
    eventTimeNs: 0,
    receivedTimeNs: 0,
    rsiValue: undefined,
    stochasticRsiValue: undefined,
    stochasticValue: undefined,
  };
}

export const AnalyticEvent: MessageFns<AnalyticEvent> = {
  encode(message: AnalyticEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.eventTimeNs !== 0) {
      writer.uint32(16).int64(message.eventTimeNs);
    }
    if (message.receivedTimeNs !== 0) {
      writer.uint32(24).int64(message.receivedTimeNs);
    }
    if (message.rsiValue !== undefined) {
      RsiValue.encode(message.rsiValue, writer.uint32(34).fork()).join();
    }
    if (message.stochasticRsiValue !== undefined) {
      StochasticRsiValue.encode(message.stochasticRsiValue, writer.uint32(42).fork()).join();
    }
    if (message.stochasticValue !== undefined) {
      StochasticValue.encode(message.stochasticValue, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyticEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyticEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.eventTimeNs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.receivedTimeNs = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rsiValue = RsiValue.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.stochasticRsiValue = StochasticRsiValue.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.stochasticValue = StochasticValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyticEvent {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      eventTimeNs: isSet(object.eventTimeNs) ? globalThis.Number(object.eventTimeNs) : 0,
      receivedTimeNs: isSet(object.receivedTimeNs) ? globalThis.Number(object.receivedTimeNs) : 0,
      rsiValue: isSet(object.rsiValue) ? RsiValue.fromJSON(object.rsiValue) : undefined,
      stochasticRsiValue: isSet(object.stochasticRsiValue)
        ? StochasticRsiValue.fromJSON(object.stochasticRsiValue)
        : undefined,
      stochasticValue: isSet(object.stochasticValue) ? StochasticValue.fromJSON(object.stochasticValue) : undefined,
    };
  },

  toJSON(message: AnalyticEvent): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.eventTimeNs !== 0) {
      obj.eventTimeNs = Math.round(message.eventTimeNs);
    }
    if (message.receivedTimeNs !== 0) {
      obj.receivedTimeNs = Math.round(message.receivedTimeNs);
    }
    if (message.rsiValue !== undefined) {
      obj.rsiValue = RsiValue.toJSON(message.rsiValue);
    }
    if (message.stochasticRsiValue !== undefined) {
      obj.stochasticRsiValue = StochasticRsiValue.toJSON(message.stochasticRsiValue);
    }
    if (message.stochasticValue !== undefined) {
      obj.stochasticValue = StochasticValue.toJSON(message.stochasticValue);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnalyticEvent>, I>>(base?: I): AnalyticEvent {
    return AnalyticEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnalyticEvent>, I>>(object: I): AnalyticEvent {
    const message = createBaseAnalyticEvent();
    message.symbol = object.symbol ?? "";
    message.eventTimeNs = object.eventTimeNs ?? 0;
    message.receivedTimeNs = object.receivedTimeNs ?? 0;
    message.rsiValue = (object.rsiValue !== undefined && object.rsiValue !== null)
      ? RsiValue.fromPartial(object.rsiValue)
      : undefined;
    message.stochasticRsiValue = (object.stochasticRsiValue !== undefined && object.stochasticRsiValue !== null)
      ? StochasticRsiValue.fromPartial(object.stochasticRsiValue)
      : undefined;
    message.stochasticValue = (object.stochasticValue !== undefined && object.stochasticValue !== null)
      ? StochasticValue.fromPartial(object.stochasticValue)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
