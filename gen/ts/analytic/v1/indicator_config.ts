// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: analytic/v1/indicator_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { RsiConfig } from "./rsi";
import { StochasticConfig } from "./stochastic";
import { StochasticRsiConfig } from "./stochastic_rsi";

export const protobufPackage = "analytic.v1";

export interface IndicatorConfig {
  indicatorId: string;
  rsi?: RsiConfig | undefined;
  stochastic?: StochasticConfig | undefined;
  stochasticRsi?: StochasticRsiConfig | undefined;
}

function createBaseIndicatorConfig(): IndicatorConfig {
  return { indicatorId: "", rsi: undefined, stochastic: undefined, stochasticRsi: undefined };
}

export const IndicatorConfig: MessageFns<IndicatorConfig> = {
  encode(message: IndicatorConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indicatorId !== "") {
      writer.uint32(10).string(message.indicatorId);
    }
    if (message.rsi !== undefined) {
      RsiConfig.encode(message.rsi, writer.uint32(18).fork()).join();
    }
    if (message.stochastic !== undefined) {
      StochasticConfig.encode(message.stochastic, writer.uint32(26).fork()).join();
    }
    if (message.stochasticRsi !== undefined) {
      StochasticRsiConfig.encode(message.stochasticRsi, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndicatorConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndicatorConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indicatorId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rsi = RsiConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stochastic = StochasticConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stochasticRsi = StochasticRsiConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndicatorConfig {
    return {
      indicatorId: isSet(object.indicatorId) ? globalThis.String(object.indicatorId) : "",
      rsi: isSet(object.rsi) ? RsiConfig.fromJSON(object.rsi) : undefined,
      stochastic: isSet(object.stochastic) ? StochasticConfig.fromJSON(object.stochastic) : undefined,
      stochasticRsi: isSet(object.stochasticRsi) ? StochasticRsiConfig.fromJSON(object.stochasticRsi) : undefined,
    };
  },

  toJSON(message: IndicatorConfig): unknown {
    const obj: any = {};
    if (message.indicatorId !== "") {
      obj.indicatorId = message.indicatorId;
    }
    if (message.rsi !== undefined) {
      obj.rsi = RsiConfig.toJSON(message.rsi);
    }
    if (message.stochastic !== undefined) {
      obj.stochastic = StochasticConfig.toJSON(message.stochastic);
    }
    if (message.stochasticRsi !== undefined) {
      obj.stochasticRsi = StochasticRsiConfig.toJSON(message.stochasticRsi);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndicatorConfig>, I>>(base?: I): IndicatorConfig {
    return IndicatorConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndicatorConfig>, I>>(object: I): IndicatorConfig {
    const message = createBaseIndicatorConfig();
    message.indicatorId = object.indicatorId ?? "";
    message.rsi = (object.rsi !== undefined && object.rsi !== null) ? RsiConfig.fromPartial(object.rsi) : undefined;
    message.stochastic = (object.stochastic !== undefined && object.stochastic !== null)
      ? StochasticConfig.fromPartial(object.stochastic)
      : undefined;
    message.stochasticRsi = (object.stochasticRsi !== undefined && object.stochasticRsi !== null)
      ? StochasticRsiConfig.fromPartial(object.stochasticRsi)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
