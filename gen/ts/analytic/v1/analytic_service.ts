// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: analytic/v1/analytic_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type handleServerStreamingCall,
  makeGenericClientConstructor,
  Metadata,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { AnalyticEvent } from "./analytic_event";
import { IndicatorConfig } from "./indicator_config";

export const protobufPackage = "analytic.v1";

export interface SubscribeAnalyticEventsRequest {
  symbols: string[];
  configs: IndicatorConfig[];
}

export interface SubscribeAnalyticEventsResponse {
  event: AnalyticEvent | undefined;
}

function createBaseSubscribeAnalyticEventsRequest(): SubscribeAnalyticEventsRequest {
  return { symbols: [], configs: [] };
}

export const SubscribeAnalyticEventsRequest: MessageFns<SubscribeAnalyticEventsRequest> = {
  encode(message: SubscribeAnalyticEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.symbols) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.configs) {
      IndicatorConfig.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeAnalyticEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeAnalyticEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbols.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.configs.push(IndicatorConfig.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeAnalyticEventsRequest {
    return {
      symbols: globalThis.Array.isArray(object?.symbols) ? object.symbols.map((e: any) => globalThis.String(e)) : [],
      configs: globalThis.Array.isArray(object?.configs)
        ? object.configs.map((e: any) => IndicatorConfig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SubscribeAnalyticEventsRequest): unknown {
    const obj: any = {};
    if (message.symbols?.length) {
      obj.symbols = message.symbols;
    }
    if (message.configs?.length) {
      obj.configs = message.configs.map((e) => IndicatorConfig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeAnalyticEventsRequest>, I>>(base?: I): SubscribeAnalyticEventsRequest {
    return SubscribeAnalyticEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeAnalyticEventsRequest>, I>>(
    object: I,
  ): SubscribeAnalyticEventsRequest {
    const message = createBaseSubscribeAnalyticEventsRequest();
    message.symbols = object.symbols?.map((e) => e) || [];
    message.configs = object.configs?.map((e) => IndicatorConfig.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSubscribeAnalyticEventsResponse(): SubscribeAnalyticEventsResponse {
  return { event: undefined };
}

export const SubscribeAnalyticEventsResponse: MessageFns<SubscribeAnalyticEventsResponse> = {
  encode(message: SubscribeAnalyticEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      AnalyticEvent.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeAnalyticEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeAnalyticEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = AnalyticEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeAnalyticEventsResponse {
    return { event: isSet(object.event) ? AnalyticEvent.fromJSON(object.event) : undefined };
  },

  toJSON(message: SubscribeAnalyticEventsResponse): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = AnalyticEvent.toJSON(message.event);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeAnalyticEventsResponse>, I>>(base?: I): SubscribeAnalyticEventsResponse {
    return SubscribeAnalyticEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeAnalyticEventsResponse>, I>>(
    object: I,
  ): SubscribeAnalyticEventsResponse {
    const message = createBaseSubscribeAnalyticEventsResponse();
    message.event = (object.event !== undefined && object.event !== null)
      ? AnalyticEvent.fromPartial(object.event)
      : undefined;
    return message;
  },
};

export type AnalyticServiceService = typeof AnalyticServiceService;
export const AnalyticServiceService = {
  subscribeAnalyticEvents: {
    path: "/analytic.v1.AnalyticService/SubscribeAnalyticEvents",
    requestStream: false,
    responseStream: true,
    requestSerialize: (value: SubscribeAnalyticEventsRequest) =>
      Buffer.from(SubscribeAnalyticEventsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => SubscribeAnalyticEventsRequest.decode(value),
    responseSerialize: (value: SubscribeAnalyticEventsResponse) =>
      Buffer.from(SubscribeAnalyticEventsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => SubscribeAnalyticEventsResponse.decode(value),
  },
} as const;

export interface AnalyticServiceServer extends UntypedServiceImplementation {
  subscribeAnalyticEvents: handleServerStreamingCall<SubscribeAnalyticEventsRequest, SubscribeAnalyticEventsResponse>;
}

export interface AnalyticServiceClient extends Client {
  subscribeAnalyticEvents(
    request: SubscribeAnalyticEventsRequest,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<SubscribeAnalyticEventsResponse>;
  subscribeAnalyticEvents(
    request: SubscribeAnalyticEventsRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<SubscribeAnalyticEventsResponse>;
}

export const AnalyticServiceClient = makeGenericClientConstructor(
  AnalyticServiceService,
  "analytic.v1.AnalyticService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AnalyticServiceClient;
  service: typeof AnalyticServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
