// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: analytic/v1/rsi.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Interval, intervalFromJSON, intervalToJSON } from "../../common/v1/enums";

export const protobufPackage = "analytic.v1";

export interface RsiConfig {
  period: number;
  interval: Interval;
}

export interface RsiValue {
  value: number;
}

function createBaseRsiConfig(): RsiConfig {
  return { period: 0, interval: 0 };
}

export const RsiConfig: MessageFns<RsiConfig> = {
  encode(message: RsiConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.period !== 0) {
      writer.uint32(8).int32(message.period);
    }
    if (message.interval !== 0) {
      writer.uint32(16).int32(message.interval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RsiConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRsiConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.period = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RsiConfig {
    return {
      period: isSet(object.period) ? globalThis.Number(object.period) : 0,
      interval: isSet(object.interval) ? intervalFromJSON(object.interval) : 0,
    };
  },

  toJSON(message: RsiConfig): unknown {
    const obj: any = {};
    if (message.period !== 0) {
      obj.period = Math.round(message.period);
    }
    if (message.interval !== 0) {
      obj.interval = intervalToJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RsiConfig>, I>>(base?: I): RsiConfig {
    return RsiConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RsiConfig>, I>>(object: I): RsiConfig {
    const message = createBaseRsiConfig();
    message.period = object.period ?? 0;
    message.interval = object.interval ?? 0;
    return message;
  },
};

function createBaseRsiValue(): RsiValue {
  return { value: 0 };
}

export const RsiValue: MessageFns<RsiValue> = {
  encode(message: RsiValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== 0) {
      writer.uint32(9).double(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RsiValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRsiValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.value = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RsiValue {
    return { value: isSet(object.value) ? globalThis.Number(object.value) : 0 };
  },

  toJSON(message: RsiValue): unknown {
    const obj: any = {};
    if (message.value !== 0) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RsiValue>, I>>(base?: I): RsiValue {
    return RsiValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RsiValue>, I>>(object: I): RsiValue {
    const message = createBaseRsiValue();
    message.value = object.value ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
