// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: analytic/v1/stochastic_rsi.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Interval, intervalFromJSON, intervalToJSON } from "../../common/v1/enums";

export const protobufPackage = "analytic.v1";

export interface StochasticRsiConfig {
  rsiPeriod: number;
  stochPeriod: number;
  kSmoothing: number;
  dSmoothing: number;
  interval: Interval;
}

export interface StochasticRsiValue {
  kLine: number;
  dLine: number;
}

function createBaseStochasticRsiConfig(): StochasticRsiConfig {
  return { rsiPeriod: 0, stochPeriod: 0, kSmoothing: 0, dSmoothing: 0, interval: 0 };
}

export const StochasticRsiConfig: MessageFns<StochasticRsiConfig> = {
  encode(message: StochasticRsiConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rsiPeriod !== 0) {
      writer.uint32(8).int32(message.rsiPeriod);
    }
    if (message.stochPeriod !== 0) {
      writer.uint32(16).int32(message.stochPeriod);
    }
    if (message.kSmoothing !== 0) {
      writer.uint32(24).int32(message.kSmoothing);
    }
    if (message.dSmoothing !== 0) {
      writer.uint32(32).int32(message.dSmoothing);
    }
    if (message.interval !== 0) {
      writer.uint32(40).int32(message.interval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StochasticRsiConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStochasticRsiConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rsiPeriod = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stochPeriod = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kSmoothing = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.dSmoothing = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StochasticRsiConfig {
    return {
      rsiPeriod: isSet(object.rsiPeriod) ? globalThis.Number(object.rsiPeriod) : 0,
      stochPeriod: isSet(object.stochPeriod) ? globalThis.Number(object.stochPeriod) : 0,
      kSmoothing: isSet(object.kSmoothing) ? globalThis.Number(object.kSmoothing) : 0,
      dSmoothing: isSet(object.dSmoothing) ? globalThis.Number(object.dSmoothing) : 0,
      interval: isSet(object.interval) ? intervalFromJSON(object.interval) : 0,
    };
  },

  toJSON(message: StochasticRsiConfig): unknown {
    const obj: any = {};
    if (message.rsiPeriod !== 0) {
      obj.rsiPeriod = Math.round(message.rsiPeriod);
    }
    if (message.stochPeriod !== 0) {
      obj.stochPeriod = Math.round(message.stochPeriod);
    }
    if (message.kSmoothing !== 0) {
      obj.kSmoothing = Math.round(message.kSmoothing);
    }
    if (message.dSmoothing !== 0) {
      obj.dSmoothing = Math.round(message.dSmoothing);
    }
    if (message.interval !== 0) {
      obj.interval = intervalToJSON(message.interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StochasticRsiConfig>, I>>(base?: I): StochasticRsiConfig {
    return StochasticRsiConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StochasticRsiConfig>, I>>(object: I): StochasticRsiConfig {
    const message = createBaseStochasticRsiConfig();
    message.rsiPeriod = object.rsiPeriod ?? 0;
    message.stochPeriod = object.stochPeriod ?? 0;
    message.kSmoothing = object.kSmoothing ?? 0;
    message.dSmoothing = object.dSmoothing ?? 0;
    message.interval = object.interval ?? 0;
    return message;
  },
};

function createBaseStochasticRsiValue(): StochasticRsiValue {
  return { kLine: 0, dLine: 0 };
}

export const StochasticRsiValue: MessageFns<StochasticRsiValue> = {
  encode(message: StochasticRsiValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kLine !== 0) {
      writer.uint32(9).double(message.kLine);
    }
    if (message.dLine !== 0) {
      writer.uint32(17).double(message.dLine);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StochasticRsiValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStochasticRsiValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.kLine = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.dLine = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StochasticRsiValue {
    return {
      kLine: isSet(object.kLine) ? globalThis.Number(object.kLine) : 0,
      dLine: isSet(object.dLine) ? globalThis.Number(object.dLine) : 0,
    };
  },

  toJSON(message: StochasticRsiValue): unknown {
    const obj: any = {};
    if (message.kLine !== 0) {
      obj.kLine = message.kLine;
    }
    if (message.dLine !== 0) {
      obj.dLine = message.dLine;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StochasticRsiValue>, I>>(base?: I): StochasticRsiValue {
    return StochasticRsiValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StochasticRsiValue>, I>>(object: I): StochasticRsiValue {
    const message = createBaseStochasticRsiValue();
    message.kLine = object.kLine ?? 0;
    message.dLine = object.dLine ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
